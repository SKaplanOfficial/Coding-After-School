void setup(){
  size(200,200);
  background(0);
}

void draw(){
// Needed only to have program repeat
}

void mousePressed(){ // When mouse is pressed, display a white background
  background(255);
}

void mouseReleased(){ // When mouse is released, display a black background
  background(0);
}
